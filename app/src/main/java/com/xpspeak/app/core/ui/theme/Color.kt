package com.xpspeak.app.core.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta definida en el Cap. 4.5 (Interfaces gráficas de usuario):
// Azules = confianza/tecnología (base y estructura)
// Naranja/Amarillo = energía/recompensa/logro (elementos interactivos, XP, rachas)
// Neutros = contenido y tipografía

// --- Tonos azules (base y estructura) ---
val XPBluePrimary = Color(0xFF1565C0)
val XPBlueDark = Color(0xFF0D47A1)
val XPBlueLight = Color(0xFFBBDEFB)

// --- Acentos cálidos (recompensa, XP, rachas) ---
val XPOrangeAccent = Color(0xFFFB8C00)
val XPYellowAccent = Color(0xFFFDD835)

// --- Neutros (contenido y tipografía) ---
val XPNeutralWhite = Color(0xFFFFFFFF)
val XPNeutralGrayLight = Color(0xFFF5F5F5)
val XPNeutralGrayDark = Color(0xFF212121)
